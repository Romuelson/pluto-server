import { Controller } from '@nestjs/common';

@Controller('instance-store')
export class InstanceStoreController {
	// constructor(private readonly instanceStoreService: InstanceStoreService) {}
	// @ApiOperation({ summary: EOpenApiSummary.create })
	// @ApiResponse({ status: HttpStatus.CREATED, type: InstanceStore })
	// @UsePipes(new ValidationPipe({ transform: true }))
	// @Post()
	// async create(@Body() DTO: CreateInstanceStoreDTO) {
	// 	return await this.instanceStoreService.create(DTO);
	// }
}
