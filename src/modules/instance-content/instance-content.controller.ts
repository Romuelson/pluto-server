import { Controller } from '@nestjs/common';

@Controller('instance-content')
export class InstanceContentController {
	// constructor(private readonly instanceContentService: InstanceContentService) {}
	// @ApiOperation({ summary: EOpenApiSummary.create })
	// @ApiResponse({ status: HttpStatus.CREATED, type: InstanceContent })
	// @UsePipes(new ValidationPipe({ transform: true }))
	// @Post()
	// async create(@Body() DTO: CreateInstanceContentDTO) {
	// 	return await this.instanceContentService.create(DTO);
	// }
}
